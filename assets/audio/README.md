/**
 * Offline splash ambient — soft major pad + gentle chime (~3.2s).
 * Replace with your own `splash.mp3` / `splash.wav` if desired; keep the same path
 * or update the require() in BrandSplash.
 */
